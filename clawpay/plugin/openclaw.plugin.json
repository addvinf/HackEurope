{
  "id": "clawpay",
  "name": "ClawPay",
  "description": "Secure payment layer — lets OpenClaw make purchases with spending guardrails.",
  "skills": ["./skills"],
  "configSchema": {
    "type": "object",
    "properties": {
      "apiUrl": { "type": "string", "default": "https://clawpay.tech" },
      "apiToken": { "type": "string", "default": "" }
    },
    "additionalProperties": false
  }
}
